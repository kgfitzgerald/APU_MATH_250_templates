# follow instructions from Lesson 2 https://shiny.rstudio.com/tutorial/written-tutorial/lesson2/
# instead of editing your hello-shiny app.R from Lesson 1 (they call this App-1/app.R), 
# use the starter code below

library(shiny)

# Define UI ----
ui <- fluidPage(
  
)

# Define server logic ----
server <- function(input, output) {
  
}

# Run the app ----
shinyApp(ui = ui, server = server)